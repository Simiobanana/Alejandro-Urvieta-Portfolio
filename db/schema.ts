import { sql } from "drizzle-orm";
import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const projects = sqliteTable("projects", { id: integer("id").primaryKey({ autoIncrement:true }), title:text("title").notNull(), category:text("category").notNull().default("Otro"), description:text("description").notNull().default(""), outcome:text("outcome").notNull().default("Proyecto personal"), stack:text("stack").notNull().default("[]"), href:text("href").notNull().default("#"), repo:text("repo"), mediaUrl:text("media_url"), mediaType:text("media_type"), featured:integer("featured",{mode:"boolean"}).notNull().default(false), published:integer("published",{mode:"boolean"}).notNull().default(true), sortOrder:integer("sort_order").notNull().default(100), createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
