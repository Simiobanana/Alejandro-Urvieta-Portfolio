import { asc } from "drizzle-orm";
import { getDb } from "../../../db";
import { projects } from "../../../db/schema";
import { requireEditor } from "../../../lib/editor-auth";
export async function GET(){try{const rows=await getDb().select().from(projects).orderBy(asc(projects.sortOrder),asc(projects.id));return Response.json({projects:rows.map(p=>({...p,stack:JSON.parse(p.stack||"[]")}))});}catch(error){return Response.json({projects:[],error:error instanceof Error?error.message:"Database unavailable"});}}
export async function POST(request:Request){const access=await requireEditor();if(!access.ok)return Response.json({error:access.message},{status:access.status});const body=await request.json() as Record<string,unknown>;const title=String(body.title??"").trim(),description=String(body.description??"").trim();if(!title||!description)return Response.json({error:"Título y descripción son obligatorios."},{status:400});const tags=Array.isArray(body.stack)?body.stack.map(String).filter(Boolean):String(body.stack??"").split(",").map(v=>v.trim()).filter(Boolean);const [project]=await getDb().insert(projects).values({title,description,category:String(body.category??"Otro"),outcome:String(body.outcome??"Proyecto personal"),stack:JSON.stringify(tags),href:String(body.href??"#"),repo:body.repo?String(body.repo):null,mediaUrl:body.mediaUrl?String(body.mediaUrl):null,mediaType:body.mediaType?String(body.mediaType):null,featured:Boolean(body.featured),published:body.published!==false}).returning();return Response.json({project:{...project,stack:tags}},{status:201});}
